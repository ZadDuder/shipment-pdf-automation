# Changelog

## 2026-07-30

### Исправлено

- MOROCCANOIL-парсер адаптирован к новому F&O-формату поставщика: invoice и
  packing читаются из структурированных PDF-таблиц с переносами SKU.
- Packing продолжает package-state между страницами с разным числом колонок;
  `Qty`, `Piece`, `Weight`, `Total box` берутся с правого края строки. Поэтому
  число `14.1` из описания `M408BVCL400` больше не принимается за quantity.
- Добавлено чтение нового batch XLSX, где технические поля находятся в
  колонках `ContainerId1`–`Qty`, а не в первой строке листа.
- Invoice обрабатывается table-first: fallback по текстовому слою запускается
  только когда товарная таблица в PDF не распознана. Fallback восстанавливает
  в том числе перенесённый SKU `M408BVPW` + `7L750` как
  `M408BVPW7L750`.
- Manifest сохраняет внутренние пробелы в именах файлов, при этом
  канонический путь по-прежнему обязан находиться внутри `input_dir`.
- Batch quantity сопоставляется с packing по SKU и паллете и определяется как
  коробки либо штуки. Строки без batch number сохраняют количество, вес и
  паллету, но оставляют реквизиты партии пустыми.
- Исправлен fallback поиска файлов `MO Packing Slip ILSO...pdf` и
  `batch-ILSO... (1).xlsx`.
- MOROCCANOIL build-нода сохраняет `Total,$ = 0` для FOC, не превращает
  `boxes=0` в одну коробку и распределяет batch-строки только по
  соответствующей паллете/SSCC.
- Исходник `Build Moroccanoil Customs and CZ Rows` вынесен в
  `workflows/build_moroccanoil.js` и синхронизируется с `final.json`.
- Добавлены регрессии нового и старого форматов и сквозной Node-проверяющий
  скрипт.
- DGD зафиксирован как отдельный тип документа: его нельзя передавать в роли
  invoice или packing.
- Replacement invoice `ILSO000008323` и packing `ILSO000005313` связываются
  одним комплектом `ILSO000008323` согласно сопроводительному guide.
- Старый формат продолжает поддерживаться, но при расхождении наборов и
  компонентов между invoice и packing система не создаёт предположительных
  соответствий. Поэтому у архивного `8250699` остаётся известная разница
  `0.01` между invoice-total и суммой построчно округлённых ЧЗ-строк:
  остаток нельзя безопасно назначить компоненту без правила поставщика.

### Проверка

- На реальных полных комплектах проверены:
  - `ILSO000003204`: 103 customs / 105 ЧЗ, 7 848 шт., 220 коробок;
  - `ILSO000000570`: 2 customs / 3 ЧЗ, 948 шт., 20 коробок, итоговая цена 0;
  - `ILSO000000580`: 50 customs / 50 ЧЗ, 400 шт., 0 коробок, итоговая цена 0;
  - `ILSO000004437`: 1 customs / 1 ЧЗ, 6 шт., скидка 96, итоговая цена 0;
  - `ILSO000008323`: 1 customs / 1 ЧЗ, 372 шт., 31 коробка.
- Node.js-проверка подтвердила контрольные значения quantity, стоимости,
  скидки, коробок и веса для всех пяти новых комплектов. Старый архивный
  комплект `8251014` продолжает распознаваться без изменений.

### Production

- Релиз установлен 2026-07-30 с timestamp `20260730T124949Z`.
- Созданы резервные копии parser и SQLite n8n:
  - `/data/moil/parse_moroccanoil_bundle.py.bak.moroc-fno-20260730T124949Z`;
  - `/opt/n8n/.n8n/database.sqlite.backup-moroc-fno-20260730T124949Z`.
- Установленный parser имеет SHA-256
  `a1a8b939ff339b464f07f0db98bf9894983ae2e08fedd6d4fb1d2e4ea3ff487a`;
  MOROCCANOIL build-нода —
  `b4ccb951f6e058bf35db7ff53c3b5819f7c1ef279819cf1434b5d5f8b32813d2`.
- `PRAGMA integrity_check = ok`, `n8n` и `tg-upload-bot` активны,
  `/healthz` возвращает `{"status":"ok"}`.
- Установленный parser через production virtualenv повторно обработал три
  новых полных комплекта и старый `8251014`; все четыре прогона завершились
  без предупреждений. Клиентские документы после smoke-теста удалены из
  `/tmp`.
- Тестовый webhook не вызывался, поэтому релиз не создавал и не изменял
  клиентские Google Sheets и не отправлял Telegram-сообщения.

## 2026-07-28

### Исправлено

- Исправлена неверная интерпретация результата multi-invoice обработки,
  внедрённая 2026-07-27.
- Полный пакет по-прежнему загружается один раз: все invoice, один общий
  packing и не более одного общего batch-файла.
- Результат снова соответствует рабочему процессу клиента: для каждого
  invoice создаётся отдельная пара `<invoiceNo>` / `ЧЗ <invoiceNo>`.
- Общий packing и batch распределяются между invoice без повторного
  использования одной packing-строки; исправления SSCC/`IN CB`, FOC,
  смешанных единиц batch и компонентов наборов сохранены.
- Расшифровка набора в ЧЗ теперь повторяется непосредственно под каждой
  строкой набора, разделённой по паллете/batch. Компоненты сохраняют порядок
  invoice, получают количество своего блока и не дублируют денежные суммы.
- Создание, очистка и запись всех customs/ЧЗ пар выполняются пакетными
  запросами Google Sheets API.

### Production

- Дополнение с блочной расшифровкой наборов развёрнуто с backup n8n SQLite
  `20260728T110308Z`; `PRAGMA integrity_check = ok`.
- Execution `50` на полном `LOAD0006732` успешно прошёл parser, build и запись
  всех customs/ЧЗ листов. Финальная Telegram-нода ожидаемо отклонена из-за
  тестового `chat_id=0`.
- В `ЧЗ 126022812` подтверждены три блока набора: quantity `96` / pallet `14`,
  `576` / `15`, `304` / `16`. Под каждой строкой находятся
  `BAGM26TRAVEL`, `M101UM25`, `M107COHY70`, `M107SHHY70`, `M201HCM40`
  с количеством соответствующего блока и правильными batch.
- Повторная полная сверка 14 production-вкладок с расчётом из реальных
  документов дала `0` расхождений; `ЧЗ 126022812` содержит 18 строк.
- Исправление развёрнуто с резервной копией n8n SQLite
  (`20260728T091138Z`); `PRAGMA integrity_check = ok`.
- Два контролируемых прогона полного комплекта `LOAD0006732` (executions
  `47` и `48`) успешно прошли parser, build, создание, очистку и запись
  вкладок. Финальная Telegram-нода ожидаемо отклонена из-за тестового
  `chat_id=0`, поэтому клиенту не отправлялось служебное сообщение.
- В Google Sheets подтверждены 7 активных пар `<invoiceNo>` /
  `ЧЗ <invoiceNo>`. Ошибочный общий лист `ЧЗ LOAD0006732` переименован в
  `_ARCHIVE_WRONG_COMBINED_CZ_LOAD0006732_20260728` и скрыт.
- Для `M101LT100` подтверждено разделение между индивидуальными парами:
  `126022814` — 5 шт., boxes 0, 1.35 кг, паллета 13, batch `14477BZ`,
  total 0; `126022816` — 240 шт., boxes 5, 69.1 кг, паллета 9,
  batch `14734LDZ`, total 2126.4.
- `n8n` и `tg-upload-bot` активны, n8n `/healthz` возвращает HTTP 200 /
  `{"status":"ok"}`.
- Локально пройдены 49 тестов, JS syntax check, Python compileall и ручной
  прогон реального комплекта; reviewer agent не нашёл блокирующих замечаний.

## 2026-07-27

### Исправлено

- Контракт MOIL приведён к фактическому процессу клиента: один запуск принимает
  полный пакет из одного или нескольких invoice, ровно одного общего packing и
  не более одного batch-файла.
- В этом релизе ЧЗ ошибочно формировался одним общим листом на поставку.
  Контракт отменён исправлением от 2026-07-28.
- Создание, очистка и запись нескольких customs-листов выполняются пакетными
  запросами Google Sheets API, без отдельного полного workflow-прогона на
  каждый invoice.
- Batch сопоставляется с выбранной строкой packing не только по SSCC, но и по
  вложенному `IN CB`. Это исправляет `M101LT100`: 5 FOC-шт. относятся к
  CB `100005636`/паллете 13, а 240 шт. — к SSCC `100004392`/паллете 9.
- Выбранные packing-строки потребляются ровно один раз между invoice; равные
  quantity в нескольких invoice больше не дублируют одну и ту же паллету.
- Несколько packing-строк SKU с общим идентификатором паллеты агрегируются
  перед пересчётом batch из коробок в штуки.
- Shipping Data Requirements автоматически различает quantity в коробках и
  quantity в штуках.
- Batch-строки компонентов наборов связываются с component-строками invoice по
  описанию компонента и больше не размножают родительский SKU набора.
- Добавлены регрессии полного multi-invoice сценария, вложенного CB,
  смешанных единиц batch и набора.

### Production

- Обновление развёрнуто с backup parser, bot validation и n8n SQLite
  (`20260727T145610Z`); `PRAGMA integrity_check = ok`.
- Контролируемый прогон `LOAD0006732` обработал 7 invoice, 1 packing и
  1 batch, создал 7 customs-листов и общий `ЧЗ LOAD0006732`.
- Значения `M101LT100` сверены чтением из Google Sheets:
  `126022814` — 5 шт., boxes 0, 1.35 кг, паллета 13, batch `14477BZ`;
  `126022816` — 240 шт., boxes 5, 69.1 кг, паллета 9, batch `14734LDZ`.
- Старые индивидуальные листы `ЧЗ 126022...` переименованы с префиксом
  `_ARCHIVE_OLD_CZ_` и скрыты. Данные не удалялись.
- `n8n` и `tg-upload-bot` активны, `/healthz` возвращает `ok`.

## 2026-07-24

### Исправлено

- Добавлено распознавание нового PDF-формата invoice и packing list F&O в
  MOIL-ветке.
- Восстанавливаются SKU, перенесённые поставщиком на следующую строку.
- Строка логистического сбора `DGR` исключается из товарных позиций с
  диагностическим предупреждением.
- FOC-позиции сохраняют нулевую итоговую стоимость и полную коммерческую
  скидку.
- Строки состава набора сохраняют порядок, остаются без номера и не
  дублируют денежные суммы родительского набора.
- На тот момент был зафиксирован временный контракт `1 invoice + 1 packing`.
  Уточнение клиента от 2026-07-27 заменило его полным multi-invoice пакетом.
- Telegram-бот и Code-нода n8n отклоняют нарушение контракта, а parser
  дополнительно помечает несовпадение количества документов.
- Добавлена поддержка нового Shipping Data Requirements XLSX: динамический
  поиск заголовка, forward-fill SKU/паллета и перевод количества коробок в
  штуки по данным packing list.
- Учтены package/SSCC, GTIN, вложенные carton box и паллеты.
- Явное `boxes=0` для вложенного товара больше не превращается в одну коробку.
- Если общий packing содержит несколько строк одного SKU для разных invoice,
  сборщик сначала выбирает единственный точный набор строк по quantity. Это
  сохраняет исходные boxes, weight и pallet конкретного invoice.
- Exact-subset поиск ограничен по числу строк и состояний. При
  неоднозначности используется пропорциональный fallback с явным
  предупреждением, без молчаливого угадывания.
- Добавлены регрессионные тесты парсера и Code-ноды n8n.

### Безопасность и сопровождение

- Git переведён на allowlist: клиентские документы и credentials не
  индексируются.
- Примеры конфигурации очищены от действующих токенов.
- Webhook больше не передаёт произвольный путь в SSH-команду: компания и номер
  поставки валидируются, серверный путь строится внутри workflow.
- Добавлена документация по локальной проверке и production-релизу.

### Production

- Обновление развёрнуто на VPS с резервными копиями parser и SQLite.
- Исправлено имя Google Sheets шаблона `TEMPLATE_CZ`.
- Ошибочный объединённый прогон `LOAD0006732` признан невалидным: созданные им
  листы переименованы с префиксом `_ARCHIVE_WRONG_` и скрыты.
- Исправлен заголовок российского артикула в `_master_catalog`: вместо
  пробельного заголовка используется `SKU RU`.
- Перед обновлением справочника создан скрытый лист
  `_BACKUP_master_catalog_20260724_before_sku_sync`. По клиентскому Excel или
  точному совпадению штрихкода перенесены 12 актуальных SKU.
- Семь invoice от 23 июля и invoice `126023953` пересобраны в отдельные пары
  листов `<invoiceNo>` / `ЧЗ <invoiceNo>`.
- На production подтверждены клиентские примеры:
  `M201HCM40` — артикул `146549`, quantity `432`, boxes `3`, weight `25.92`;
  `M105THL100` — артикул `877657`, commercial discount `97.92`, boxes `6`,
  weight `54`.
